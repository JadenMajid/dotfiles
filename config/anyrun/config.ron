Config(
  x: Fraction(0.5),
  y: Fraction(0.5),
  width: Fraction(0.3),
  height: Fraction(0.0),
  hide_icons: false,
  ignore_exclusive_zones: false,
  layer: Overlay,
  hide_plugin_info: true,
  close_on_click: true,
  show_results_immediately: true,
  max_entries: Some(15), // Show up to 15 entries
  plugins: [
    "libapplications.so",
    "libshell.so",
    "libstdin.so",
    "libwebsearch.so",
    "librink.so",
  ],
)
